<!--components/w-tab-control/w-tab-control.wxml-->
<view class="tab-control">
  <view wx:for="{{titles}}" class="tab-item {{currentIndex == index ? 'active' : ''}}" data-index="{{index}}" data-title="{{item}}"  bindtap="handleTabItem" wx:key="{{index}}">
    <text>{{item}}</text>
  </view>
</view>